{
  "name": "wcs-nodejs-project",
  "version": "0.0.1",
  "description": "Example project with NodeJS backend as API",
  "main": "server.js",
  "dependencies": {
    "bluebird": "^3.5.1",
    "express": "^4.16.3",
    "sqlite": "^2.9.1"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
